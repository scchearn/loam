# Gotchas

- Trigger → writing a qmd drift check; Mistake → assuming same-named `qmd-usage.md` files are canonical copies; Fix → they are per-skill references, so never byte-sync them unless explicitly marked shared.
