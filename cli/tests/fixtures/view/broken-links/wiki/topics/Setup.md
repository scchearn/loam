# Setup

A page whose filename stem is capitalized `Setup`, used as the target of a
lowercase `[[setup]]` link elsewhere in this fixture to exercise the
`noncanonical-link-case` diagnostic (unique case-insensitive match, wrong
case).
