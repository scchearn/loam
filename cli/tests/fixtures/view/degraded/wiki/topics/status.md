# Status

The rest of this workspace is ordinary and readable. Only
`wiki/code/corrupt.md` is deliberately broken (invalid UTF-8 bytes inside
a `.md` file), to exercise a probe that fails or returns malformed data
while the workspace itself remains readable.
